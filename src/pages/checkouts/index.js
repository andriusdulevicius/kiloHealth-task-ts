import React from 'react';
import styled from 'styled-components';

const Checkout = () => <Wrapper>checkout</Wrapper>;

export default Checkout;

const Wrapper = styled.div`
  max-width: 100rem;
  background-color: blue;
`;
